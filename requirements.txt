filterpy==1.4.5
numpy==1.23.5
onnx==1.13.0
# onnxruntime >= 1.16.0 ships CoreMLExecutionProvider for macOS arm64 (Apple Silicon).
# On Apple Silicon you may instead install the community build:
#   pip install onnxruntime-silicon
# On Linux/Windows with NVIDIA GPU, replace with:
#   pip install onnxruntime-gpu
onnxruntime>=1.16.0
opencv-contrib-python==4.6.0.66
